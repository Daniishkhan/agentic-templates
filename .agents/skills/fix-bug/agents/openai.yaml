interface:
  display_name: "Fix Bug"
  short_description: "Reproduce and resolve regressions"
  default_prompt: "Use $fix-bug to reproduce this issue with a failing test, apply a minimal fix, and verify regression coverage."
